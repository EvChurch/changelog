# Web service (default). For worker: add a second service, same repo, start command: npm run worker
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
